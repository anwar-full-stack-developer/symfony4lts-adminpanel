{% extends 'layouts/adminlte/main.html.twig' %}

{% block title %}Homepage
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="example-wrapper">
		<h1>eLearning</h1>

		Simple eLearning application to mange Course, Lesson, Quiz, Students, Admin (Teacher)<br/>
		{% if is_granted('IS_AUTHENTICATED_FULLY')==false %}
			<a href="{{ path('fos_user_registration_register') }}" class="">Register</a> OR
			<a href="{{ path('fos_user_security_login') }}" class="">Login</a>
		{% endif %}

	</div>
{% endblock %}
